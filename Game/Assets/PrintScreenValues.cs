using UnityEngine;
using System.Collections;

public class PrintScreenValues : MonoBehaviour {
	
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		print (Screen.width + " " + Screen.height);
	}
}
